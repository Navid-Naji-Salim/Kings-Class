# King's Class Frontend

React and TypeScript client for the King's Class prototype.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

Set `VITE_API_URL` in `.env` when the backend API is not running on `http://localhost:4000/api`.
